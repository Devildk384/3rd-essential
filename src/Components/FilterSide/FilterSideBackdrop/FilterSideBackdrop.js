/** @format */

import React from "react";
import "./FilterSideBackdrop.scss";

const FilterSide_Backdrop = ({ click }) => (
  <div className='backdrop' onClick={click} />
);
export default FilterSide_Backdrop;
